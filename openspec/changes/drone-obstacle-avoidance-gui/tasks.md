## 1. 状态机框架搭建

- [x] 1.1 在文件顶部定义 `enum GameState { MENU, PLAYING, PAUSED, GAME_OVER }` 全局游戏状态枚举
- [x] 1.2 定义全局 `Settings` 结构体（soundOn, volume, difficulty），并声明全局实例
- [x] 1.3 重构 `main()` 函数，使用 `switch (state)` 分发到各状态的渲染和更新函数
- [x] 1.4 创建 `main()` 中统一的消息循环，使用 EasyX 的 `peekmessage` 捕获鼠标和键盘事件

## 2. 主菜单实现

- [x] 2.1 实现 `renderMenu()` 函数，绘制主菜单背景和三个按钮（开始游戏、设置、退出）
- [x] 2.2 实现 `handleMenuInput()` 函数，处理菜单中的鼠标点击和键盘方向键/回车
- [x] 2.3 实现按钮悬停高亮效果（鼠标移入改变按钮颜色）
- [x] 2.4 连接菜单按钮到状态切换（开始游戏→PLAYING, 设置→显示设置面板, 退出→关闭程序）

## 3. 设置面板实现

- [x] 3.1 实现 `renderSettings()` 函数，绘制设置界面（音效开关、音量调节、难度选择、返回按钮）
- [x] 3.2 实现 `handleSettingsInput()` 函数，处理设置面板的鼠标点击交互
- [x] 3.3 实现音效开关切换逻辑，关联 `mciSendString` 的静音控制
- [x] 3.4 实现音量加减按钮（步进 10%，范围 0-100%）
- [x] 3.5 实现难度选择（低/中/高），选中项高亮显示
- [x] 3.6 实现"返回"按钮，切换到 MENU 状态

## 4. 游戏内 HUD

- [x] 4.1 实现 `renderHUD()` 函数，在游戏画面叠加 HUD 信息（高度、位置、时间、难度）
- [x] 4.2 在 PLAYING 状态中集成 HUD 渲染（在 `show()` 绘制完成后调用 `renderHUD()`）
- [x] 4.3 实现游戏时间计时器（从 PLAYING 开始时计时，PAUSED 时暂停）
- [x] 4.4 HUD 使用半透明背景矩形定位在屏幕右上角，不遮挡飞行区域

## 5. 暂停功能

- [x] 5.1 在 PLAYING 状态下响应 Esc 键或点击暂停按钮，切换到 PAUSED 状态
- [x] 5.2 实现 `renderPauseMenu()` 函数，显示暂停遮罩和"继续游戏""返回主菜单"按钮
- [x] 5.3 实现 `handlePauseInput()` 函数，处理暂停菜单交互
- [x] 5.4 PAUSED 状态下停止游戏逻辑更新（`updateWithoutInput` 和时间计时器暂停）

## 6. 游戏结束界面

- [x] 6.1 实现 `renderGameOver()` 函数，显示游戏结束画面、最终得分/时间和两个按钮（重新开始、返回主菜单）
- [x] 6.2 实现 `handleGameOverInput()` 函数，处理游戏结束界面的按钮点击
- [x] 6.3 "重新开始"按钮重置游戏状态并切换到 PLAYING
- [x] 6.4 "返回主菜单"按钮切换到 MENU 状态

## 7. 屏幕方向按钮

- [x] 7.1 在 PLAYING 状态的渲染中绘制四个方向按钮（上/下/左/右），固定在屏幕右下角
- [x] 7.2 实现方向按钮的鼠标点击检测，点击后更新 `uav_x` 或 `uav_y`（+/- 20 像素，含边界检查）
- [x] 7.3 点击方向按钮时播放跳转音效（复用 Jump.mp3）
- [x] 7.4 保留原有 W/A/S/D 键盘控制，与屏幕按钮并存

## 8. 难度参数集成

- [x] 8.1 根据 `Settings.difficulty` 调整无人机移动速度（低=0.5x, 中=1x, 高=1.5x）
- [x] 8.2 在 PLAYING 状态的更新逻辑中应用难度参数

## 9. 整合测试与验证

- [x] 9.1 完整走通 MENU → PLAYING → PAUSED → PLAYING → GAME_OVER → MENU 状态循环
- [x] 9.2 验证键盘 W/A/S/D 和屏幕方向按钮均可控制无人机
- [x] 9.3 验证设置面板的音效开关、音量、难度功能生效
- [x] 9.4 验证 HUD 信息实时更新且不遮挡游戏画面
